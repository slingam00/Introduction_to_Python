temperature = 50

if temperature > 65:
    print("I will go outside")
else:
    print("I will not go outside")   # Since the if-statement is false, 
                                     # the else-statement is then executed
                                     # 'I will not go outside' is then printed
